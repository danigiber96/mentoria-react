import React from 'react';
import ReactDOM from 'react-dom/client';
import HolaMundo from './components/HolaMundo/HolaMundo';

const contenedor = ReactDOM.createRoot(document.getElementById('myReactApp'));
contenedor.render(
  <HolaMundo />
);